import React from 'react'

const Modal = ({ open, onClose, children }: {open:any, onClose:any, children:any}) => {
  return (
    <div onClick={open}>
          
    </div>
  )
}

export default Modal